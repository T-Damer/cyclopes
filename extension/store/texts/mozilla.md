Select Yes, then check:
Tools that combine multiple files into a single file — esbuild bundles the JavaScript.
Any other tool that processes files and generates included files — the build scripts assemble the distribution and include the generated ONNX model.